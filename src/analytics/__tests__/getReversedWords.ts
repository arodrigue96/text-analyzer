import { getReversedWords } from "../index.js";

const text = "";
const expectedReversedText = "";
const reversedWords = getReversedWords(text);

if (reversedWords === expectedReversedText) {
  console.log("Test passed");
} else {
  console.log("Test failed");
}
