import { findPalindromWords } from "../index.js";

const text = "";
const expectedPalindrom = [""];
const palindromWords = findPalindromWords(text).join(" ");

if (expectedPalindrom === expectedPalindrom) {
  console.log("Test passed");
} else {
  console.log("Test failed");
}
